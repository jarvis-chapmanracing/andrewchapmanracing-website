# 2026-03-21

- Andrew adopted sub-agent-based model routing: main/router = GPT-5.1 Mini, writing = GPT-5.4, coding = GPT-5.3 Codex, strategist = Opus.
- Confirmed writer and coder subagent lanes worked; Opus subagent failed because Anthropic returned a low-credit billing error.
- Investigated unexpected Opus spend and found local OpenClaw heartbeat/background sessions had been using Opus 4.6 for routine maintenance work, causing substantial unintended cost. Local logs showed about $17.58 of Opus spend on 2026-03-20 UTC from this machine; 2026-03-21 UTC showed failed Opus calls with $0 successful cost.
- Andrew was upset about repeated model-routing violations and explicitly reinforced these durable preferences:
  - GPT-5.1 Mini for bulk/simple tasks, heartbeat, cron, and routine automation
  - GPT-5.4 for all human-facing writing/outreach
  - GPT-5.3 Codex for coding
  - Opus explicit-only, with a hard ceiling of $3/day maximum
- Updated local config outside workspace memory files:
  - `/home/andre/.openclaw/openclaw.json`: default primary model changed to `openai-codex/gpt-5.1-codex-mini`; subagent default model also set to GPT-5.1 Mini
  - `/home/andre/.openclaw/cron/jobs.json`: `sponsor-research-hourly` payload model explicitly set to GPT-5.1 Mini
  - `/home/andre/.openclaw/.env`: Anthropic API key added/updated from Andrew
- Attempted `openclaw gateway restart` so the new config would load; restart exited with code 1 and only surfaced a Telegram group policy doctor warning, so runtime reload may still need verification in a future session.
- Fixed the strategist/Opus lane later in the session. Root cause was not just the `.env` key - OpenClaw was actively reading Anthropic credentials from `/home/andre/.openclaw/agents/main/agent/auth-profiles.json`, and that file still held the old invalid key. After updating the active auth profile store to match the new Claude API key, the strategist lane returned output normally again.
- Also fixed the Telegram group-policy restart blocker by setting `channels.telegram.groupAllowFrom` to include Andrew's sender ID (`7161603201`), which cleared the earlier doctor warning condition.
